{
  "name": "proveedor-mailer",
  "version": "1.0.1",
  "description": "Mailer proveedores — Cloudflare Worker + GitHub Pages",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
